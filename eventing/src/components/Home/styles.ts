import { StyleSheet } from 'react-native';
import theme from '../../utils/theme';

export default StyleSheet.create({
  active: { backgroundColor: theme.primaryColor },
  inActive: { backgroundColor: '#ededfd' },
});
